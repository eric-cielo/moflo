# cli/memory

> **Inlined into `@moflo/cli` by [#598](https://github.com/eric-cielo/moflo/issues/598)** (epic [#586](https://github.com/eric-cielo/moflo/issues/586) / [ADR-0001](../adr/0001-collapse-moflo-workspace-packages.md)). The `@moflo/memory` workspace package no longer exists — its contents live at `src/modules/cli/src/memory/` and ship inside the `moflo` tarball.

The unified memory service backed by sql.js + HNSW indexing (150x-12,500x faster than brute-force vector search per ADR-006/009). Provides `MofloDbAdapter`, `HnswLite`, `ControllerRegistry`, `UnifiedMemoryService`, `LearningBridge`, `MemoryGraph`, `RvfLearningStore`, `PersistentSonaCoordinator`, plus migration tooling and the auto-memory bridge (ADR-048/049).

## Where things live

| Concern | Path |
|---------|------|
| Source | `src/modules/cli/src/memory/` |
| Public surface | `src/modules/cli/src/memory/index.ts` (re-exports adapter, indices, registry, learning bridge, graph, RVF store, migration helpers) |
| Adapter | `src/modules/cli/src/memory/moflo-db-adapter.ts` |
| HNSW (pure-TS) | `src/modules/cli/src/memory/hnsw-lite.ts` |
| HNSW (libffi) | `src/modules/cli/src/memory/hnsw-index.ts` |
| Controllers | `src/modules/cli/src/memory/controllers/` |
| Backends | `src/modules/cli/src/memory/sqljs-backend.ts`, `rvf-backend.ts`, `database-provider.ts` |
| RVF persistence | `src/modules/cli/src/memory/rvf-learning-store.ts`, `persistent-sona.ts`, `rvf-migration.ts` |
| Knowledge graph | `src/modules/cli/src/memory/memory-graph.ts` |
| Learning bridge | `src/modules/cli/src/memory/learning-bridge.ts` |
| Auto-memory bridge | `src/modules/cli/src/memory/auto-memory-bridge.ts` |
| Agent-scoped memory | `src/modules/cli/src/memory/agent-memory-scope.ts` |
| Tests | inline next to sources (e.g. `learning-bridge.test.ts`) plus `src/modules/cli/src/memory/controllers/*.test.ts` |

## Internal usage

The cli imports memory via direct relative paths (no `@moflo/memory` bare specifier, no walk-up resolver inside cli):

```ts
// From cli source (e.g. cli/src/neural/reasoning-bank.ts):
import { MofloDbAdapter, createDefaultEntry } from '../memory/index.js';

// From cli/src/memory siblings:
import { ControllerRegistry } from './controller-registry.js';
import { HnswLite } from './hnsw-lite.js';
```

The consumer-side helper `auto-memory-hook.mjs` (generated by `cli/src/init/helpers-generator.ts` and shipped at `.claude/helpers/auto-memory-hook.mjs`) resolves the inlined memory module by walking up from the consumer project to `node_modules/moflo/src/modules/cli/dist/src/memory/index.js`.
