# `@moflo/*` Workspace Collapse — Dependency Graph

Generated by `scripts/analyze-collapse-deps.mjs --src` against the **TypeScript source tree**.

Files scanned: **942**.

## Adjacency list

Each row lists the `@moflo/*` packages a given module imports (static `from`, dynamic `import()`, or string-arg `mofloImport`).

| Package | Outbound count | Imports |
|---------|----------------|---------|
| `cli` | 6 | `guidance`, `hooks`, `memory`, `neural`, `swarm`, `testing` |
| `guidance` | 1 | `hooks` |
| `hooks` | 1 | `memory` |
| `memory` | 1 | `neural` |
| `neural` | 1 | `memory` |
| `spells` | 0 | — |
| `swarm` | 0 | — |
| `testing` | 2 | `memory`, `swarm` |

## Inbound count (how many other packages import this one)

| Package | Inbound count |
|---------|---------------|
| `cli` | 0 |
| `guidance` | 1 |
| `hooks` | 2 |
| `memory` | 4 |
| `neural` | 2 |
| `spells` | 0 |
| `swarm` | 2 |
| `testing` | 1 |

## Leaves (zero outbound `@moflo/*` imports)

Safe to merge first — no relative paths to other moflo packages will need rewriting.

- `@moflo/spells`
- `@moflo/swarm`

## Early-collapse candidates (outbound ≤ 1, not trunk)

After leaves, collapse these next — single relative-path rewrite each.

- `@moflo/guidance` → `hooks`
- `@moflo/hooks` → `memory`
- `@moflo/neural` → `memory`
- `@moflo/spells` → _leaf_
- `@moflo/swarm` → _leaf_

## Trunk (≥ 3 inbound)

Collapse last — these are widely depended on, so flipping them changes many call sites.

- `@moflo/memory` (inbound = 4)

## Topological collapse order (leaves first)

Recommended merge order. Each subsequent step has all of its dependencies already merged into the root package, so its own internal `@moflo/*` imports become local relative paths.

1. `@moflo/spells`
2. `@moflo/swarm`
3. `@moflo/<cycle:cli,guidance,hooks,memory,neural,testing>`

## Root-level references

Files outside `src/modules/` (e.g. `bin/`, `src/index.ts`, `scripts/`) that reference `@moflo/*`. After collapse, these become local imports.

- `@moflo/cli`
- `@moflo/memory`
- `@moflo/neural`
- `@moflo/swarm`
- `@moflo/testing`
